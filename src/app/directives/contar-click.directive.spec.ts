import { ContarClickDirective } from './contar-click.directive';

describe('ContarClickDirective', () => {
  it('should create an instance', () => {
    const directive = new ContarClickDirective();
    expect(directive).toBeTruthy();
  });
});
